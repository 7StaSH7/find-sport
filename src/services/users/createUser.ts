// import { db } from "@database/index";
// import { usersTable } from "@database/schemas/users";

// export const createUser = async (createUser: CreateUserDto) => {
//   return db.insert(usersTable).values(createUser).returning();
// };
